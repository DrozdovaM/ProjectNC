import { ReductionFullnamePipe } from './reduction-fullname.pipe';

describe('ReductionFullnamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReductionFullnamePipe();
    expect(pipe).toBeTruthy();
  });
});
