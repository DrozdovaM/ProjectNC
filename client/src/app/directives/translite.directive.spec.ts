import { TransliteDirective } from './translite.directive';

describe('TransliteDirective', () => {
  it('should create an instance', () => {
    const directive = new TransliteDirective();
    expect(directive).toBeTruthy();
  });
});
