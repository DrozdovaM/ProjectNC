import { DeleteStudentDirective } from './delete-student.directive';

describe('DeleteStudentDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteStudentDirective();
    expect(directive).toBeTruthy();
  });
});
