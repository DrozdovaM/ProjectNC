import { EditSizeDirective } from './edit-size.directive';

describe('EditSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new EditSizeDirective();
    expect(directive).toBeTruthy();
  });
});
